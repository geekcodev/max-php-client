# Access token бота (обязательная). Передаётся в заголовке Authorization без префикса Bearer.
MAX_API_TOKEN=

# Секрет для верификации вебхуков (опциональная). 5-256 символов [a-zA-Z0-9_-].
MAX_WEBHOOK_SECRET=
